// Test: Preprocessor macros
// RUN: %blocktype -E %s 2>&1 | FileCheck %s

#define FOO 42
// CHECK: 42
int x = FOO;

#define ADD(a, b) ((a) + (b))
// CHECK: ((1) + (2))
int y = ADD(1, 2);

// CHECK: __cplusplus
int version = __cplusplus;
