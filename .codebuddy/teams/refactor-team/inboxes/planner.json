[
  {
    "id": "msg-1777098898462-o6qb2d",
    "from": "team-lead",
    "to": "planner",
    "type": "message",
    "content": "## 任务：产出 A-F6 优化版任务文档\n\n### 任务定义（来自 11-AI-Coder-任务流-PhaseA.md）\n\n**Task A-F6：StructuredDiagnostic 基础结构定义**\n\n**依赖**：无\n\n**产出文件**\n\n| 操作 | 文件路径 |\n|------|----------|\n| 新增 | `include/blocktype/IR/IRDiagnostic.h` |\n| 新增 | `src/IR/IRDiagnostic.cpp` |\n\n**必须实现的类型定义**\n\n```cpp\nnamespace blocktype::diag {\n\nenum class DiagnosticLevel : uint8_t {\n  Note = 0, Remark = 1, Warning = 2, Error = 3, Fatal = 4,\n};\n\nenum class DiagnosticGroup : uint16_t {\n  TypeMapping = 0, InstructionValidation = 1, IRVerification = 2,\n  BackendCodegen = 3, FFIBinding = 4, Serialization = 5,\n};\n\nclass StructuredDiagnostic {\n  DiagnosticLevel Level;\n  DiagnosticGroup Group;\n  ir::IRErrorCode Code;\n  std::string Message;\n  SourceLocation Loc;\n  SmallVector&lt;std::string, 4&gt; Notes;\npublic:\n  DiagnosticLevel getLevel() const { return Level; }\n  DiagnosticGroup getGroup() const { return Group; }\n  ir::IRErrorCode getCode() const { return Code; }\n  StringRef getMessage() const { return Message; }\n  void addNote(StringRef N) { Notes.push_back(N.str()); }\n  std::string toJSON() const;\n  std::string toText() const;\n};\n\nclass StructuredDiagEmitter {\npublic:\n  virtual ~StructuredDiagEmitter() = default;\n  virtual void emit(const StructuredDiagnostic&amp; D) = 0;\n};\n\nclass DiagnosticGroupManager {\n  DenseMap&lt;DiagnosticGroup, bool&gt; EnabledGroups;\npublic:\n  void enableGroup(DiagnosticGroup G) { EnabledGroups[G] = true; }\n  void disableGroup(DiagnosticGroup G) { EnabledGroups[G] = false; }\n  bool isGroupEnabled(DiagnosticGroup G) const;\n};\n\n}\n```\n\n**约束**：支持文本和 JSON 输出；DiagnosticGroupManager 控制哪些诊断组启用\n\n**验收标准**\n\n```cpp\n// V1: StructuredDiagnostic 创建\ndiag::StructuredDiagnostic D;\nD.Level = diag::DiagnosticLevel::Error;\nD.Code = ir::IRErrorCode::TypeMappingFailed;\nD.Message = \"Cannot map QualType to IRType\";\nassert(D.getLevel() == diag::DiagnosticLevel::Error);\n\n// V2: JSON 输出\nstd::string JSON = D.toJSON();\nassert(JSON.find(\"TypeMappingFailed\") != std::string::npos);\n```\n\n---\n\n### 关键背景信息（planner 必须分析）\n\n#### 1. IRErrorCode 不存在\n项目中 **没有** `ir::IRErrorCode` 枚举。这是 spec 中的前置依赖缺失。你需要：\n- 分析是否需要在 A-F6 中定义 `IRErrorCode` 枚举（A-F10 专门做这个，但 A-F6 需要用到）\n- 或者先用一个占位/简化方案（如 `uint32_t Code` 或定义最小化的 `IRErrorCode`）\n- 参考 `SerializationErrorKind`（`IRSerializer.h:21`）和 `VerificationCategory`（`IRVerifier.h:21`）的设计模式\n\n#### 2. SourceLocation 歧义\n项目中存在 **两个** SourceLocation：\n- `blocktype::Basic::SourceLocation`（`Basic/SourceLocation.h`，64-bit 编码，用于前端词法/语法分析）\n- `blocktype::ir::SourceLocation`（`IR/IRDebugMetadata.h:16`，Filename+Line+Column，用于 IR 调试信息）\n\nspec 中 `StructuredDiagnostic` 使用 `SourceLocation Loc`，你需要决定用哪个，或定义专用的诊断 SourceLocation。\n\n#### 3. DenseMap 的 Key 要求\n项目的 `ir::DenseMap`（`IR/ADT/DenseMap.h`）使用 `std::hash<KeyT>` 作为哈希函数。`DiagnosticGroup` 是 `enum class : uint16_t`，需要确认 `std::hash` 对 enum class 的支持（C++11 不保证，C++14+ 通常支持）。可能需要提供 `DenseMapInfo` 特化或使用 `static_cast<uint16_t>` 作为 key。\n\n#### 4. SmallVector 命名空间\nspec 中 `SmallVector<std::string, 4>` 使用的是 `ir::SmallVector`（定义在 `IR/ADT/SmallVector.h`，命名空间 `blocktype::ir`）。`diag` 命名空间需要通过 `using` 或完整限定名访问。\n\n#### 5. 命名空间设计\nspec 使用 `blocktype::diag`（非嵌套在 `ir` 下），这与之前的 `blocktype::ir::debug` 不同。需要确认：\n- `diag` 是顶层命名空间还是 `blocktype::ir::diag`？\n- 如果是顶层，需要独立 include 路径还是放在 `IR/` 目录下？\n\n#### 6. StructuredDiagnostic 的字段访问\nspec 中 `D.Level = diag::DiagnosticLevel::Error;` 是直接字段赋值（public），但 `getLevel()` 是 getter。需要决定字段是 public 还是 private + setter/getter。\n\n---\n\n### 红线 Checklist\n(1) 架构优先 — 诊断系统独立于前端和后端\n(2) 多前端多后端自由组合 — 不引入前后端耦合\n(3) 渐进式改造 — 新增文件为主，不改变现有接口\n(4) 现有功能不退化 — 所有现有测试必须通过\n(5) 接口抽象优先 — StructuredDiagEmitter 抽象基类\n(6) IR 中间层解耦 — 诊断信息属于 IR 层\n\n### 输出路径\n`/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/19-AI-Coder-任务流-PhaseA-AF6-优化版.md`\n\n### 必读文件列表（含绝对路径）\n1. `/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/11-AI-Coder-任务流-PhaseA.md`（A-F6 原始 spec，约 2225-2305 行）\n2. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRDebugMetadata.h`（ir::SourceLocation 定义）\n3. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRDebugInfo.h`（debug 命名空间设计参考）\n4. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRVerifier.h`（VerificationDiagnostic 设计参考）\n5. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRSerializer.h`（SerializationErrorKind 设计参考）\n6. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT.h`（ADT 类型汇总）\n7. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/DenseMap.h`（DenseMap 实现）\n8. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/SmallVector.h`（SmallVector 实现）\n9. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/StringRef.h`（StringRef 实现）\n10. `/Users/yuan/Documents/BlockType/include/blocktype/Basic/SourceLocation.h`（前端 SourceLocation）\n11. `/Users/yuan/Documents/BlockType/include/blocktype/Basic/Diagnostics.h`（现有 DiagnosticsEngine）\n12. `/Users/yuan/Documents/BlockType/src/IR/CMakeLists.txt`（构建配置）\n13. `/Users/yuan/Documents/BlockType/tests/unit/IR/CMakeLists.txt`（测试构建配置）\n\n### 文档要求\n1. 完整的代码背景分析（特别是上述 6 个关键问题）\n2. 每个设计决策的明确结论和理由\n3. 完整的头文件代码（可直接复制使用）\n4. 完整的 .cpp 实现代码\n5. 完整的测试用例代码\n6. CMakeLists.txt 修改规范\n7. 验收标准 V1~VN 的完整测试映射\n8. sizeof 影响评估\n9. 风险与注意事项",
    "timestamp": "2026-04-25T06:34:58.462Z",
    "read": true
  },
  {
    "id": "msg-1777115230979-esx49k",
    "from": "team-lead",
    "to": "planner",
    "type": "message",
    "content": "## 任务：产出 A-F7 优化版任务文档\n\n### 任务定义（来自 11-AI-Coder-任务流-PhaseA.md，2309-2431 行）\n\n**Task A-F7：CacheKey/CacheEntry 基础类型定义 + 编译器缓存架构**\n\n**依赖**：A.3.1（IRFormatVersion）— ✅ 已存在\n\n**产出文件**\n\n| 操作 | 文件路径 |\n|------|----------|\n| 新增 | `include/blocktype/IR/IRCache.h` |\n| 新增 | `src/IR/IRCache.cpp` |\n\n**必须实现的类型定义**\n\n```cpp\nnamespace blocktype::cache {\n\nstruct CacheKey {\n  uint64_t SourceHash;\n  uint64_t OptionsHash;\n  uint64_t VersionHash;\n  uint64_t TargetTripleHash;\n  uint64_t DependencyHash;\n  uint64_t CombinedHash;\n  static CacheKey compute(StringRef Source, const CompilerOptions& Opts);\n  std::string toHex() const;\n};\n\nstruct CacheEntry {\n  CacheKey Key;\n  std::vector<uint8_t> IRData;\n  std::vector<uint8_t> ObjectData;\n  ir::IRFormatVersion IRVersion;\n  uint64_t Timestamp;\n  size_t IRSize;\n  size_t ObjectSize;\n};\n\nclass CacheStorage {\npublic:\n  virtual ~CacheStorage() = default;\n  virtual Optional&lt;CacheEntry&gt; lookup(const CacheKey&amp; Key) = 0;\n  virtual bool store(const CacheKey&amp; Key, const CacheEntry&amp; Entry) = 0;\n  struct Stats { size_t Hits; size_t Misses; size_t Evictions; size_t TotalSize; };\n  virtual Stats getStats() const = 0;\n};\n\nclass LocalDiskCache : public CacheStorage {\n  std::string CacheDir;\n  size_t MaxSize;\npublic:\n  explicit LocalDiskCache(StringRef Dir, size_t Max);\n  Optional&lt;CacheEntry&gt; lookup(const CacheKey&amp; Key) override;\n  bool store(const CacheKey&amp; Key, const CacheEntry&amp; Entry) override;\n  Stats getStats() const override;\n  void evictIfNeeded();\n};\n\nclass RemoteCache : public CacheStorage {\n  std::string Endpoint;\n  std::string Bucket;\npublic:\n  Optional&lt;CacheEntry&gt; lookup(const CacheKey&amp; Key) override;\n  bool store(const CacheKey&amp; Key, const CacheEntry&amp; Entry) override;\n};\n\nclass CompilationCacheManager {\n  std::unique_ptr&lt;CacheStorage&gt; Storage;\n  bool Enabled = false;\npublic:\n  void enable(StringRef CacheDir, size_t MaxSize);\n  void disable();\n  Optional&lt;std::unique_ptr&lt;ir::IRModule&gt;&gt; lookupIR(const CacheKey&amp; Key, ir::IRTypeContext&amp; Ctx);\n  Optional&lt;std::vector&lt;uint8_t&gt;&gt; lookupObject(const CacheKey&amp; Key);\n  bool storeIR(const CacheKey&amp; Key, const ir::IRModule&amp; M);\n  bool storeObject(const CacheKey&amp; Key, ArrayRef&lt;uint8_t&gt; Data);\n};\n\n}\n```\n\n**缓存文件命名规则**\n```\n目录结构：<CacheDir>/<first2hex>/<rest38hex>/\n  - IR缓存：<CacheDir>/ab/cdef0123456789.../ir.btir\n  - 目标缓存：<CacheDir>/ab/cdef0123456789.../obj.o\n  - 元数据：<CacheDir>/ab/cdef0123456789.../meta.json\n```\n\n**编译选项**\n```\n--fcache-dir=<path>           指定编译缓存目录\n--fcache-size=<size>          最大缓存大小（如 \"10G\"）\n--fcache-remote=<url>         远程缓存端点（远期）\n--fcache-no                   禁用编译缓存\n--fcache-stats                输出缓存统计\n```\n\n**约束**：CacheKey 使用 BLAKE3 稳定哈希；LocalDiskCache LRU淘汰；RemoteCache 远期实现\n\n**验收标准**\n```cpp\n// V1: CacheKey 计算\ncache::CacheKey Key = cache::CacheKey::compute(\"int main(){}\", Opts);\nassert(Key.CombinedHash != 0);\n\n// V2: LocalDiskCache 存取\ncache::LocalDiskCache LDC(\"/tmp/btcache\", 1024*1024*1024);\ncache::CacheEntry Entry;\nEntry.Key = Key;\nEntry.IRData = {1,2,3};\nassert(LDC.store(Key, Entry));\nauto Found = LDC.lookup(Key);\nassert(Found.has_value());\n```\n\n---\n\n### 关键背景信息（planner 必须分析）\n\n#### 1. CompilerOptions 不存在\nspec 中 `CacheKey::compute(StringRef Source, const CompilerOptions& Opts)` 引用了 `CompilerOptions`，但项目中**没有**这个类型。你需要：\n- 决定是否定义一个简化的 `CompilerOptions` / `CacheOptions` 结构体\n- 或者简化 `CacheKey::compute` 的签名（如接受各 hash 分量）\n- 参考 `CacheKey` 的 5 个 hash 字段（SourceHash/OptionsHash/VersionHash/TargetTripleHash/DependencyHash），决定 compute 如何生成它们\n\n#### 2. Optional 类型\nspec 使用 `Optional<T>`，但项目没有自定义 Optional。A-F5/A-F6 中已统一使用 `std::optional<T>`。需要统一。\n\n#### 3. IRTypeContext 引用\nspec 中 `CompilationCacheManager::lookupIR` 需要 `ir::IRTypeContext&`。该类型存在于 `include/blocktype/IR/IRTypeContext.h`。但 A-F7 是基础类型定义阶段，`lookupIR` 需要反序列化 IR，这可能超出\"基础定义\"的范围。需要决定：\n- 是提供完整实现还是 stub/placeholder？\n- `CompilationCacheManager` 的 `lookupIR`/`storeIR` 是否应该在 A-F7 实现？\n\n#### 4. BLAKE3 哈希\nspec 约束\"CacheKey 使用 BLAKE3 稳定哈希\"。项目中没有 BLAKE3 实现。你需要：\n- 决定是引入 BLAKE3 第三方库还是用简单的哈希替代（如 FNV-1a / cityhash）\n- 考虑 Phase A 是基础定义阶段，hash 算法是否可后续替换\n\n#### 5. LocalDiskCache 文件 I/O\nspec 要求完整的磁盘缓存实现（目录结构、LRU 淘汰）。在 A-F7 基础定义阶段：\n- 是否提供完整实现？\n- 还是 stub + TODO 注释？\n\n#### 6. RemoteCache 远期实现\nspec 注明\"RemoteCache 远期实现\"。需要决定是 stub 还是完全省略。\n\n#### 7. 命名空间设计\nspec 使用 `blocktype::cache`（非嵌套在 `ir` 下）。与 `diag` 类似的顶层命名空间设计。\n\n#### 8. IRModule 反序列化依赖\n`CompilationCacheManager::lookupIR` 需要 IRModule 反序列化能力（依赖 `IRSerializer`）。如果完整实现，会增加大量依赖。考虑是否简化。\n\n---\n\n### 红线 Checklist\n(1) 架构优先 — 缓存系统独立于前端和后端\n(2) 多前端多后端自由组合 — 不引入前后端耦合\n(3) 渐进式改造 — 新增文件为主，不改变现有接口\n(4) 现有功能不退化 — 所有现有测试必须通过\n(5) 接口抽象优先 — CacheStorage 抽象基类\n(6) IR 中间层解耦 — 缓存属于 IR 层基础设施\n\n### 输出路径\n`/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/20-AI-Coder-任务流-PhaseA-AF7-优化版.md`\n\n### 必读文件列表（含绝对路径）\n1. `/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/11-AI-Coder-任务流-PhaseA.md`（A-F7 原始 spec，约 2309-2431 行）\n2. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRFormatVersion.h`（依赖项）\n3. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRModule.h`（IRModule 定义）\n4. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRTypeContext.h`（IRTypeContext）\n5. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRSerializer.h`（序列化接口）\n6. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT.h`（ADT 类型汇总）\n7. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/StringRef.h`（StringRef）\n8. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/ArrayRef.h`（ArrayRef）\n9. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT/DenseMap.h`（DenseMap）\n10. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRDiagnostic.h`（命名空间设计参考：blocktype::diag）\n11. `/Users/yuan/Documents/BlockType/src/IR/CMakeLists.txt`（构建配置）\n12. `/Users/yuan/Documents/BlockType/tests/unit/IR/CMakeLists.txt`（测试构建配置）\n\n### 文档要求\n1. 完整的代码背景分析（特别是上述 8 个关键问题）\n2. 每个设计决策的明确结论和理由\n3. 完整的头文件代码（可直接复制使用）\n4. 完整的 .cpp 实现代码\n5. 完整的测试用例代码\n6. CMakeLists.txt 修改规范\n7. 验收标准 V1~VN 的完整测试映射\n8. sizeof 影响评估\n9. 风险与注意事项",
    "timestamp": "2026-04-25T11:07:10.979Z",
    "read": true
  },
  {
    "id": "msg-1777116208200-sb11xf",
    "from": "team-lead",
    "to": "planner",
    "type": "message",
    "content": "## 任务：产出 A-F8 优化版任务文档\n\n### 任务定义（来自 11-AI-Coder-任务流-PhaseA.md，2435-2516 行）\n\n**Task A-F8：IRIntegrityChecksum 基础实现 + IRSigner + 可重现构建**\n\n**依赖**：A.4（IRModule）— ✅ 已存在\n\n**产出文件**\n\n| 操作 | 文件路径 |\n|------|----------|\n| 新增 | `include/blocktype/IR/IRIntegrity.h` |\n| 新增 | `src/IR/IRIntegrity.cpp` |\n\n**必须实现的类型定义**\n\n```cpp\nnamespace blocktype::ir::security {\n\nstruct IRIntegrityChecksum {\n  uint64_t TypeSystemHash;\n  uint64_t InstructionHash;\n  uint64_t ConstantHash;\n  uint64_t GlobalHash;\n  uint64_t DebugInfoHash;\n  uint64_t CombinedHash;\n  static IRIntegrityChecksum compute(const IRModule&amp; M);\n  bool verify(const IRModule&amp; M) const;\n};\n\nusing PrivateKey = std::array&lt;uint8_t, 32&gt;;\nusing PublicKey  = std::array&lt;uint8_t, 32&gt;;\nusing Signature  = std::array&lt;uint8_t, 64&gt;;\n\nclass IRSigner {\npublic:\n  static Signature sign(const IRModule&amp; M, const PrivateKey&amp; Key);\n  static bool verify(const IRModule&amp; M, const Signature&amp; Sig, const PublicKey&amp; Key);\n};\n\n}\n```\n\n**可重现构建行为列表**\n\n1. 时间戳：使用 SOURCE_DATE_EPOCH 环境变量值（默认0）\n2. 随机数：所有随机数生成器使用确定性种子（seed=0x42）\n3. 遍历顺序：所有哈希表遍历改为排序后遍历（按key排序）\n4. 内部计数器：ValueID/InstructionID使用确定性起始值（从1递增）\n5. 临时文件名：使用确定性命名（而非随机后缀）\n6. DWARF生产者字符串：使用固定版本号（不含构建时间）\n\n**编译选项**\n```\n--freproducible-build          启用可重现构建模式\n--fir-integrity-check          加载IR时验证完整性\n--fir-sign=<key-file>          对输出IR签名\n--fir-verify-signature=<key>   验证输入IR签名\n```\n\n**约束**：哈希算法使用 BLAKE3；签名算法使用 Ed25519；IRModule 已有 IsReproducible 字段和 computeChecksum() 方法\n\n**验收标准**\n```cpp\n// V1: IRIntegrityChecksum 计算\nir::security::IRIntegrityChecksum Chk = ir::security::IRIntegrityChecksum::compute(Module);\nassert(Chk.CombinedHash != 0);\n\n// V2: 校验和验证\nassert(Chk.verify(Module) == true);\n\n// V3: 可重现构建\nModule.setReproducible(true);\nauto Chk1 = ir::security::IRIntegrityChecksum::compute(Module);\nauto Chk2 = ir::security::IRIntegrityChecksum::compute(Module);\nassert(Chk1.CombinedHash == Chk2.CombinedHash);\n```\n\n---\n\n### 关键背景信息（planner 必须分析）\n\n#### 1. computeChecksum() 方法不存在\nspec 约束说\"IRModule 已有 computeChecksum() 方法\"，但实际 `IRModule` 中**没有**这个方法。`IRModule` 仅有 `isReproducible()`/`setReproducible()`。`IRIntegrityChecksum::compute()` 是独立的 static 方法，直接操作 `IRModule` 的数据。\n\n#### 2. BLAKE3 哈希算法不可用\n项目中没有 BLAKE3 实现（A-F7 已决定用 FNV-1a 替代 CacheKey 的 hash）。需要决定：\n- A-F8 是否继续用 FNV-1a？\n- 或者引入更安全的哈希（SHA-256 等）？\n- 需要考虑 `IRIntegrityChecksum` 是用于**完整性校验**（安全性需求），而非缓存键（性能需求）\n\n#### 3. Ed25519 签名算法不可用\n项目中没有 Ed25519 实现。需要决定：\n- IRSigner 是 stub 还是完整实现？\n- 如果 stub，如何处理 sign/verify 的返回值？\n- 是否需要引入第三方加密库？\n\n#### 4. IRModule 数据访问\n`IRIntegrityChecksum::compute` 需要遍历 IRModule 的所有组件（Functions/FunctionDecls/Globals/Aliases/Metadata）。需要检查这些组件的 API 是否支持遍历。IRModule 提供：\n- `getFunctions()` 返回 `vector<unique_ptr<IRFunction>>&`\n- `getGlobals()` 返回 `vector<unique_ptr<IRGlobalVariable>>&`\n- 但 `FunctionDecls`/`Aliases`/`Metadata` 没有公开的 getter\n\n#### 5. 可重现构建行为\nspec 列出了 6 种可重现构建行为。这些行为的实现涉及多个系统组件，A-F8 作为基础定义阶段，需要决定：\n- 哪些行为在 A-F8 实现？\n- 哪些仅定义接口，后续 Phase 实现？\n\n#### 6. 命名空间设计\nspec 使用 `blocktype::ir::security`（嵌套在 `ir` 下）。这与 A-F6 的 `blocktype::diag` 和 A-F7 的 `blocktype::cache`（顶层命名空间）不同。\n\n---\n\n### 红线 Checklist\n(1) 架构优先 — 完整性校验独立于前端和后端\n(2) 多前端多后端自由组合 — 不引入前后端耦合\n(3) 渐进式改造 — 新增文件为主，不改变现有接口\n(4) 现有功能不退化 — 所有现有测试必须通过\n(5) 接口抽象优先 — IRSigner 提供静态接口\n(6) IR 中间层解耦 — 完整性校验属于 IR 层\n\n### 输出路径\n`/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/21-AI-Coder-任务流-PhaseA-AF8-优化版.md`\n\n### 必读文件列表（含绝对路径）\n1. `/Users/yuan/Documents/BlockType/docs/plan/多前端后端重构方案/11-AI-Coder-任务流-PhaseA.md`（A-F8 原始 spec，约 2435-2516 行）\n2. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRModule.h`（IRModule 完整定义，151 行）\n3. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRFunction.h`（IRFunction）\n4. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRType.h`（IRType 类型系统）\n5. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRConstant.h`（IRConstant）\n6. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRBasicBlock.h`（IRBasicBlock）\n7. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRInstruction.h`（IRInstruction，含 DbgInfo）\n8. `/Users/yuan/Documents/BlockType/include/blocktype/IR/ADT.h`（ADT 类型汇总）\n9. `/Users/yuan/Documents/BlockType/include/blocktype/IR/IRCache.h`（A-F7 FNV-1a hash 参考）\n10. `/Users/yuan/Documents/BlockType/src/IR/CMakeLists.txt`（构建配置）\n11. `/Users/yuan/Documents/BlockType/tests/unit/IR/CMakeLists.txt`（测试构建配置）\n\n### 文档要求\n1. 完整的代码背景分析（特别是上述 6 个关键问题）\n2. 每个设计决策的明确结论和理由\n3. 完整的头文件代码（可直接复制使用）\n4. 完整的 .cpp 实现代码\n5. 完整的测试用例代码\n6. CMakeLists.txt 修改规范\n7. 验收标准 V1~VN 的完整测试映射\n8. sizeof 影响评估\n9. 风险与注意事项",
    "timestamp": "2026-04-25T11:23:28.200Z",
    "read": true
  }
]